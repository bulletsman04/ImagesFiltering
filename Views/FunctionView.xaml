<UserControl x:Class="Views.FunctionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Image 
            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type Grid}, Mode=FindAncestor}}" 
            Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type Grid}, Mode=FindAncestor}}" 
            Source="{Binding ImageSource, Mode=OneWay}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" 
            Stretch="Fill"
            Name="CustomFunction"
            MouseDown="CustomFunction_OnMouseDown"
            MouseMove="CustomFunction_OnMouseMove"
            MouseUp="CustomFunction_OnMouseUp"
        >
        </Image>
    </Grid>
</UserControl>
